import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromRGBO(46, 65, 89, 0.85);
  static const Color secundary = Color.fromRGBO(242, 135, 5, 1);
  static const Color accent = Color.fromARGB(255, 255, 196, 0);

  // Backgrounds
  static const Color background = Color.fromRGBO(46, 65, 89, 0.85);
  static const Color backgroundComponent = Color.fromARGB(255, 58, 62, 141);
  static const Color backgroundComponentSelected = Color.fromARGB(255, 62, 102, 147);
  static const Color backgroundInput = Color.fromRGBO(255, 255, 255, 0.15);

  // Foregrouds
  static const Color foreground = Color.fromRGBO(238, 238, 238, 1);
}
